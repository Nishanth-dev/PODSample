import React, { Component } from 'react';
import NavigationInApp from '../../navigation';

export default class Root extends Component {
  render() {
    return (
      <NavigationInApp />
    );
  }
}